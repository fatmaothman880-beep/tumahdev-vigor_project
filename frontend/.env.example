# Base URL of the future FastAPI backend.
VITE_API_URL=http://localhost:8000

# Set to "true" (default) to run entirely on mock data — no backend required.
# Set to "false" once the FastAPI backend described in src/api is available.
VITE_USE_MOCK_API=true
